import 'package:flutter/material.dart';

class TennisWidget extends StatelessWidget {
   
  const TennisWidget({Key? key}) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('TennisWidget'),
      ),
    );
  }
}